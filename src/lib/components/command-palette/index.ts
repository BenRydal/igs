// Only the component is consumed via this barrel; types and helpers are
// imported by direct path from ./types, ./fuzzy-search, ./recent-commands.
export { default as CommandPalette } from './CommandPalette.svelte'
